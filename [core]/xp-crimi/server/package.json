{
  "name": "@xp-crimi/server",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf ./dist && rm -rf ./node_modules",
    "--------- BUILD ---------": "--------- BUILD ---------",
    "build": "rollup -c",
    "--------- WATCH ---------": "--------- WATCH ---------",
    "watch": "rollup -c -w --watch.include src/*.ts"
  },
  "dependencies": {
    "@lib/citizenfx-utils": "workspace:*",
    "@lib/database": "workspace:*"
  },
  "devDependencies": {
    "@citizenfx/server": "catalog:",
    "@tools/rollup": "workspace:*",
    "@types/node": "catalog:",
    "rollup": "catalog:",
    "tslib": "catalog:",
    "typescript": "catalog:",
    "@xp-crimi/types": "workspace:*"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9.5"
  }
}
